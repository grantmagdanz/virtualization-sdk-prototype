name: Pre-commit

on: [issues, pull_request]

jobs:
  test-tools:
    name: Test the dvp-tools package

    runs-on: ${{ matrix.os }}
    strategy:
      max-parallel: 4
      matrix:
        python-version: [2.7]
        os: [ubuntu-latest, macOS-latest]

    steps:
    - uses: actions/checkout@v1
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      working-directory: tools
      run: |
        python -m pip install --upgrade pip
        pip install -r lock.requirements.txt -r lock.dev-requirements.txt
        
    - name: Test with pytest
      working-directory: tools
      run: |
        pip install pytest
        PYTHONPATH=src/main/python pytest src/test/python/
        

  test-libs:
    name: Test the dvp-libs package

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [2.7]

    steps:
    - uses: actions/checkout@v1
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      working-directory: libs
      run: |
        python -m pip install --upgrade pip
        pip install -r lock.requirements.txt -r lock.dev-requirements.txt
        
    - name: Test with pytest
      working-directory: libs
      run: |
        pip install pytest
        PYTHONPATH=src/main/python pytest src/test/python/


  test-platform:
    name: Test the dvp-platform package

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [2.7]

    steps:
    - uses: actions/checkout@v1
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      working-directory: platform
      run: |
        python -m pip install --upgrade pip
        pip install -r lock.requirements.txt -r lock.dev-requirements.txt
        
    - name: Test with pytest
      working-directory: platform
      run: |
        pip install pytest
        PYTHONPATH=src/main/python pytest src/test/python/


  test-common:
    name: Test the dvp-common package

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [2.7]

    steps:
    - uses: actions/checkout@v1
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      working-directory: common
      run: |
        python -m pip install --upgrade pip
        pip install -r lock.requirements.txt -r lock.dev-requirements.txt
        
    - name: Test with pytest
      working-directory: common
      run: |
        pip install pytest
        PYTHONPATH=src/main/python pytest src/test/python/
